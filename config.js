/* Tip Tap Games — backend config.
 *
 * Paste your Supabase Project URL and anon key here (Settings -> API).
 * Both are PUBLIC values, safe to commit — row-level security is what protects
 * the data, not secrecy of this key.
 *
 * NEVER put the service_role key here. It bypasses RLS.
 *
 * While these are blank the app runs on a seeded local mock backend, so the
 * feed, leaderboard and sign-in prompt all work offline.
 */

window.TIPTAP_CONFIG = {
  SUPABASE_URL: 'https://qinbnmxmgcvhuxqtclmj.supabase.co',
  SUPABASE_ANON_KEY: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InFpbmJubXhtZ2N2aHV4cXRjbG1qIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODUwMDAxNTYsImV4cCI6MjEwMDU3NjE1Nn0.iru27JI-jqWCF3o3vBqIZ6cMlw9Ms3qRmJFPFXrk_6A'
};
